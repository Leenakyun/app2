<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IRIS MBTI TEST - Decision Tree Edition</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <style>
        :root {
            --primary-color: #2d4a36; /* 포레스트 그린 */
            --bg-color: #f7f6f0; /* 샌드 크림 복고조 */
            --card-bg: #ffffff;
            --text-main: #2d3748;
            --text-muted: #718096;
            --accent-purple: #7b68ee; /* 버시컬러 */
            --accent-orange: #ed8936; /* 버지니카 */
            --accent-blue: #3182ce; /* 세토사 */
        }

        * { box-sizing: border-box; font-family: 'Noto Sans KR', sans-serif; }
        
        body {
            background-color: #e2e8f0;
            margin: 0; padding: 0;
            display: flex; justify-content: center; align-items: center;
            min-height: 100vh;
        }

        /* 스마트폰 뷰포트 컨테이너 */
        .phone-wrapper {
            width: 100%;
            max-width: 412px;
            height: 92vh;
            max-height: 892px;
            background-color: var(--bg-color);
            border-radius: 36px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            overflow: hidden;
            border: 12px solid #1a202c;
            position: relative;
            display: flex;
            flex-direction: column;
        }

        /* 상단 앱바 */
        .app-header {
            background-color: var(--primary-color);
            color: white;
            padding: 24px 20px 16px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom-left-radius: 20px;
            border-bottom-right-radius: 20px;
        }
        .app-title { font-weight: 700; font-size: 1.1rem; letter-spacing: 0.5px; display: flex; align-items: center; gap: 8px; }
        .base-badge { background: rgba(255,255,255,0.2); padding: 4px 10px; border-radius: 12px; font-size: 0.75rem; font-weight: 500; }

        /* 메인 콘텐츠 영역 */
        .content-scroll {
            flex: 1;
            overflow-y: auto;
            padding: 20px;
            display: flex;
            flex-direction: column;
        }

        /* 컴포넌트 공통 카드 */
        .card {
            background: var(--card-bg);
            border-radius: 18px;
            padding: 20px;
            margin-bottom: 16px;
            box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05);
            border: 1px solid rgba(0,0,0,0.03);
        }

        /* 화면 스위칭 시스템 */
        .screen { display: none; height: 100%; flex-direction: column; }
        .screen.active { display: flex; }

        /* 인트로 스크린 */
        #intro-screen { text-align: center; justify-content: center; align-items: center; }
        .logo-icon { font-size: 4rem; color: var(--primary-color); margin-bottom: 20px; animation: float 3s ease-in-out infinite; }
        @keyframes float { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }
        
        /* 프로그레스 바 (Step Indicator) */
        .step-container { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; }
        .step-text { font-size: 0.85rem; font-weight: 700; color: var(--primary-color); }
        .step-bar-bg { width: 100%; height: 6px; background: #e2e8f0; border-radius: 3px; margin-top: 8px; overflow: hidden; }
        .step-bar-fill { height: 100%; background: var(--primary-color); width: 25%; transition: width 0.3s ease; }

        /* 질문지 UI */
        .question-title { font-size: 1.3rem; font-weight: 700; color: var(--text-main); line-height: 1.4; margin-bottom: 24px; }
        .slider-box { background: #f8fafc; border-radius: 14px; padding: 20px; text-align: center; }
        .slider-score { font-size: 1.1rem; font-weight: 700; color: var(--primary-color); margin-bottom: 12px; }
        .custom-slider { width: 100%; accent-color: var(--primary-color); margin: 15px 0; }
        .slider-labels { display: flex; justify-content: space-between; font-size: 0.75rem; color: var(--text-muted); line-height: 1.3; }
        .slider-labels span { width: 45%; }

        /* 결과 페이지 UI */
        .result-header { text-align: center; padding: 10px 0; }
        .result-tag { font-size: 0.85rem; font-weight: 700; color: var(--text-muted); letter-spacing: 1px; }
        .result-type { font-size: 1.6rem; font-weight: 700; color: var(--primary-color); margin: 8px 0; }
        .result-flower-tag { display: inline-flex; align-items: center; gap: 6px; background: #f0fff4; color: #38a169; padding: 6px 14px; border-radius: 20px; font-size: 0.85rem; font-weight: 500; }

        /* SVG 의사결정트리 패스 트래커 */
        .tree-tracker-box { background: #2d3748; color: white; position: relative; padding: 15px; border-radius: 14px; margin-top: 10px; }
        .tree-node-visual { font-size: 0.8rem; fill: #a0aec0; font-weight: bold; }
        .tree-node-visual.highlight { fill: #68d391; font-size: 0.85rem; }
        .tree-edge { stroke: #4a5568; stroke-width: 2; stroke-dasharray: 4; }
        .tree-edge.highlight { stroke: #48bb78; stroke-width: 4; stroke-dasharray: 0; }

        /* 하단 공통 푸터/버튼 바 */
        .app-footer { padding: 15px 20px 25px 20px; background: var(--bg-color); display: flex; gap: 12px; }
        .btn { flex: 1; border: none; padding: 16px; border-radius: 14px; font-size: 1rem; font-weight: 700; cursor: pointer; display: flex; justify-content: center; align-items: center; gap: 8px; transition: all 0.2s; }
        .btn-primary { background: var(--primary-color); color: white; }
        .btn-primary:hover { background: #223829; }
        .btn-secondary { background: #e2e8f0; color: var(--text-main); }

        /* 가짜 브라우저 주소창 필터 (올려주신 스크린샷 감성 재현) */
        .mock-browser-url { background: #edf2f7; padding: 10px; font-size: 0.85rem; text-align: center; color: #4a5568; border-top: 1px solid #e2e8f0; display: flex; justify-content: center; align-items: center; gap: 6px; }
    </style>
</head>
<body>

<div class="phone-wrapper">
    <!-- 앱 헤더 -->
    <div class="app-header">
        <div class="app-title"><i class="fa-solid fa-seedling"></i> IRIS MBTI TEST</div>
        <div class="base-badge"><i class="fa-solid fa-database"></i> ML-Tree.js</div>
    </div>

    <!-- 콘텐츠 영역 -->
    <div class="content-scroll">
        
        <!-- SCREEN 1: 인트로 -->
        <div id="intro-screen" class="screen active">
            <i class="fa-solid fa-circle-nodes logo-icon"></i>
            <h2 style="color: var(--primary-color); margin-bottom: 10px;">인공지능 붓꽃 성향 테스트</h2>
            <p style="color: var(--text-muted); font-size: 0.95rem; margin-bottom: 30px; padding: 0 10px;">
                머신러닝 <b>의사결정트리(Decision Tree)</b> 알고리즘이 당신의 성격 성향 데이터를 기반으로 매핑되는 실시간 분기 경로를 추적합니다.
            </p>
            <button class="btn btn-primary" onclick="startTest()" style="width: 100%;">알고리즘 트랙 시작하기 <i class="fa-solid fa-chevron-right"></i></button>
        </div>

        <!-- SCREEN 2: 질문지 문항 스위칭 -->
        <div id="quiz-screen" class="screen">
            <div class="step-container">
                <span class="step-text" id="step-indicator">STEP 1 OF 4</span>
                <span id="feature-type-badge" style="font-size: 0.75rem; color: var(--text-muted); font-weight: 500;">꽃잎 길이 프레임</span>
            </div>
            <div class="step-bar-bg">
                <div class="step-bar-fill" id="step-progress"></div>
            </div>
            
            <div class="content-area" style="flex:1; display:flex; flex-direction:column; justify-content:center;">
                <div class="question-title" id="question-text">새로운 무언가를 시작할 때, 당신의 행동 성향은?</div>
                
                <div class="slider-box">
                    <div class="slider-score" id="slider-val-display">5점 (중립형 성향)</div>
                    <input type="range" min="1" max="9" value="5" class="custom-slider" id="mbti-slider" oninput="onSliderChange(this.value)">
                    <div class="slider-labels">
                        <span id="label-left" style="text-align: left;">생각나면 즉시 행동!<br>(무계획 돌격형)</span>
                        <span id="label-right" style="text-align: right;">치밀하고 꼼꼼한 플래닝<br>(철저한 계획형)</span>
                    </div>
                </div>
            </div>

            <div class="app-footer" style="padding: 20px 0 0 0;">
                <button class="btn btn-primary" onclick="nextQuestion()">다음 질문 <i class="fa-solid fa-chevron-right"></i></button>
            </div>
        </div>

        <!-- SCREEN 3: 결과 화면 -->
        <div id="result-screen" class="screen">
            <div class="card result-header">
                <span class="result-tag">YOUR IRIS TYPE IS</span>
                <div class="result-type" id="res-type-title">어디서나 스며드는 만능 '버시컬러'형</div>
                <div class="result-flower-tag"><i class="fa-solid fa-flower"></i> <span id="res-flower-meaning">꽃말: 기쁜 소식, 다채로운 아름다움</span></div>
            </div>

            <div class="card" style="font-size: 0.9rem; color: var(--text-main); line-height: 1.6;">
                <h4 style="margin: 0 0 8px 0; color: var(--primary-color);"><i class="fa-solid fa-user-check"></i> 당신의 꽃잎형 성향 분석</h4>
                <p id="res-description" style="margin: 0;">낯설고 척박한 땅이든 유연하고 탁월한 적응력을 지닌 성향입니다. 내면의 밸런스가 뛰어나 환경에 구애받지 않고 유연한 사교성을 자랑합니다.</p>
            </div>

            <!-- 의사결정나무 실시간 추적 패스 다이어그램 (SVG 핵심 구현) -->
            <div class="card tree-tracker-box">
                <h4 style="margin: 0 0 12px 0; font-size: 0.85rem; color: #68d391;"><i class="fa-solid fa-network-wired"></i> 머신러닝 의사결정트리 분기 실시간 추적</h4>
                <svg viewBox="0 0 340 180" style="width: 100%; height: auto;">
                    <!-- 루트 노드에서 뻗어나가는 라인들 -->
                    <line x1="170" y1="25" x2="85" y2="80" class="tree-edge" id="edge-to-setosa" />
                    <line x1="170" y1="25" x2="255" y2="80" class="tree-edge" id="edge-to-node2" />
                    <line x1="255" y1="80" x2="210" y2="145" class="tree-edge" id="edge-to-versicolor" />
                    <line x1="255" y1="80" x2="300" y2="145" class="tree-edge" id="edge-to-virginica" />

                    <!-- 노드 원형 및 텍스트 렌더링 -->
                    <!-- Root Node -->
                    <circle cx="170" cy="25" r="6" fill="#fff" />
                    <text x="170" y="15" text-anchor="middle" class="tree-node-visual highlight">[조건 1] 꽃잎 길이 ≤ 2.45</text>

                    <!-- Leaf: Setosa -->
                    <circle cx="85" cy="80" r="6" fill="#4a5568" id="node-setosa" />
                    <text x="85" y="95" text-anchor="middle" class="tree-node-visual" id="txt-setosa">SETOSA 형 (100%)</text>

                    <!-- Sub Node 2 -->
                    <circle cx="255" cy="80" r="6" fill="#4a5568" id="node-sub2" />
                    <text x="255" y="70" text-anchor="middle" class="tree-node-visual" id="txt-sub2">[조건 2] 꽃잎 너비 ≤ 1.75</text>

                    <!-- Leaf: Versicolor -->
                    <circle cx="210" cy="145" r="6" fill="#4a5568" id="node-versicolor" />
                    <text x="210" y="160" text-anchor="middle" class="tree-node-visual" id="txt-versicolor">VERSICOLOR 형</text>

                    <!-- Leaf: Virginica -->
                    <circle cx="300" cy="145" r="6" fill="#4a5568" id="node-virginica" />
                    <text x="300" y="160" text-anchor="middle" class="tree-node-visual" id="txt-virginica">VIRGINICA 형</text>
                </svg>
            </div>

            <div class="app-footer" style="padding: 20px 0 0 0;">
                <button class="btn btn-secondary" onclick="location.reload()"><i class="fa-solid fa-rotate-left"></i> 다시하기</button>
                <button class="btn btn-primary" onclick="alert('결과 복사가 완료되었습니다!')"><i class="fa-solid fa-copy"></i> 결과 복사</button>
            </div>
        </div>

    </div>

    <!-- 주소창 감성 푸터 -->
    <div class="mock-browser-url">
        <i class="fa-solid fa-lock" style="font-size: 0.75rem; color: #38a169;"></i>
        <span>ny0874-debug.github.io</span>
    </div>
</div>

<script>
    // 4가지 요소를 담을 유저 성향 수치 매핑 (머신러닝 인풋으로 스케일 변환)
    // 붓꽃 원본 데이터 피처 순서: [꽃받침 길이, 꽃받침 너비, 꽃잎 길이, 꽃잎 너비]
    let currentStep = 0;
    let userAnswers = [5, 5, 5, 5];

    const questions = [
        {
            title: "새로운 무언가를 시작할 때, 당신의 행동 성향은?",
            left: "생각나면 즉시 행동!<br>(무계획 돌격형)",
            right: "치밀하고 꼼꼼한 플래닝<br>(철저한 계획형)",
            badge: "꽃받침 길이 (Sepal Length) 매핑"
        },
        {
            title: "타인과의 논쟁이나 토론이 벌어졌을 때 당신의 스탠스는?",
            left: "내 주장을 굽히지 않는다<br>(공격형 리더십)",
            right: "상대의 논리에 유연하게 맞춘다<br>(수용형 서포터)",
            badge: "꽃받침 너비 (Sepal Width) 매핑"
        },
        {
            title: "예기치 못한 트러블이나 변수가 발생했을 때 멘탈 반응은?",
            left: "멘탈이 파스스 부서진다<br>(예민하고 섬세함)",
            right: "그럴 수 있지 하고 무던하다<br>(대범하고 무감각함)",
            badge: "꽃잎 길이 (Petal Length) 매핑"
        },
        {
            title: "친한 친구가 우울해서 슬픈 감정을 털어놓을 때 당신의 위로법은?",
            left: "폭풍 공감과 감정 케어<br>(F형 감성 교감)",
            right: "원인을 분석하고 솔루션 제시<br>(T형 이성 해결)",
            badge: "꽃잎 너비 (Petal Width) 매핑"
        }
    ];

    function startTest() {
        document.getElementById('intro-screen').classList.remove('active');
        document.getElementById('quiz-screen').classList.add('active');
        loadQuestion();
    }

    function loadQuestion() {
        const q = questions[currentStep];
        document.getElementById('step-indicator').textContent = `STEP ${currentStep + 1} OF 4`;
        document.getElementById('feature-type-badge').textContent = q.badge;
        document.getElementById('question-text').textContent = q.title;
        document.getElementById('label-left').innerHTML = q.left;
        document.getElementById('label-right').innerHTML = q.right;
        
        // 슬라이더 초기화
        document.getElementById('mbti-slider').value = userAnswers[currentStep];
        onSliderChange(userAnswers[currentStep]);

        // 프로그레스바 변형
        document.getElementById('step-progress').style.width = `${(currentStep + 1) * 25}%`;
    }

    function onSliderChange(val) {
        let text = `${val}점 `;
        if (val < 4) text += "(외향/직관 성향 편향)";
        else if (val > 6) text += "(내향/이성 성향 편향)";
        else text += "(균형 잡힌 유연한 조율자)";
        document.getElementById('slider-val-display').textContent = text;
        userAnswers[currentStep] = parseInt(val);
    }

    function nextQuestion() {
        if (currentStep < 3) {
            currentStep++;
            loadQuestion();
        } else {
            calculateDecisionTreeResult();
        }
    }

    // 💡 핵심: 사이킷런에서 학습시킨 의사결정트리 분기 규칙 알고리즘 자바스크립트 이식 완료
    function calculateDecisionTreeResult() {
        document.getElementById('quiz-screen').classList.remove('active');
        document.getElementById('result-screen').classList.add('active');

        // MBTI 입력 스코어(1~9)를 실제 아이리스 데이터 피처 변수 스케일(cm)로 수학적 스케일링 변환
        const sepal_length = 4.3 + (userAnswers[0] - 1) * (3.6 / 8); 
        const sepal_width  = 2.0 + (userAnswers[1] - 1) * (2.4 / 8);
        const petal_length = 1.0 + (userAnswers[2] - 1) * (5.9 / 8); // 조건 1용 가중치
        const petal_width  = 0.1 + (userAnswers[3] - 1) * (2.4 / 8); // 조건 2용 가중치

        // 모든 하이라이트 CSS 클래스 초기화
        document.querySelectorAll('.tree-edge, .tree-node-visual, circle').forEach(el => el.classList.remove('highlight'));

        let finalType = "";
        let flowerMeaning = "";
        let description = "";

        // 트리 추적 시각화 하이라이트 매핑 로직
        // 조건 1단계: 꽃잎 길이 수치 검증
        if (petal_length <= 2.45) {
            finalType = "조용히 빛나는 내유외강 '세토사'형";
            flowerMeaning = "꽃말: 수줍음, 신비로운 고결함";
            description = "당신은 세토사 품종을 닮아 자극에 쉽게 동요하지 않고 자신만의 굳건한 세계를 지키는 내유외강형 인물입니다. 사색과 디테일에 강하며 타인에게 신비로운 아우라를 전달합니다.";
            
            // SVG 하이라이트 라인 연결
            document.getElementById('edge-to-setosa').classList.add('highlight');
            document.getElementById('node-setosa').classList.add('highlight');
            document.getElementById('txt-setosa').classList.add('highlight');
        } 
        // 2단계 분기 진입
        else {
            document.getElementById('edge-to-node2').classList.add('highlight');
            document.getElementById('node-sub2').classList.add('highlight');
            document.getElementById('txt-sub2').classList.add('highlight');

            // 조건 2단계: 꽃잎 너비 수치 검증
            if (petal_width <= 1.75) {
                finalType = "어디서나 스며드는 만능 적응러 '버시컬러'형";
                flowerMeaning = "꽃말: 기쁜 소식, 다채로운 아름다움";
                description = "당신의 성향은 주변의 조화와 균형을 극대화하는 버시컬러형입니다. 낯설고 척박한 조직 환경이든 뛰어난 생존력과 유연함으로 금세 적응해 주변에 긍정적인 에너지를 전파합니다.";

                document.getElementById('edge-to-versicolor').classList.add('highlight');
                document.getElementById('node-versicolor').classList.add('highlight');
                document.getElementById('txt-versicolor').classList.add('highlight');
            } else {
                finalType = "압도적인 존재감의 화려한 리더 '버지니카'형";
                flowerMeaning = "꽃말: 큰 기쁨, 당당한 승리";
                description = "당신은 생물학적 고유 한계치까지 당당하게 발달하는 대형 버지니카형입니다. 리더십이 강하고 큰 변수 앞에서도 덤덤하며, 중요한 순간에 집단을 이끄는 강력한 추진력을 보여줍니다.";

                document.getElementById('edge-to-virginica').classList.add('highlight');
                document.getElementById('node-virginica').classList.add('highlight');
                document.getElementById('txt-virginica').classList.add('highlight');
            }
        }

        // 결과 값 화면 DOM 갱신
        document.getElementById('res-type-title').textContent = finalType;
        document.getElementById('res-flower-meaning').textContent = flowerMeaning;
        document.getElementById('res-description').textContent = description;
    }
</script>

</body>
</html>
